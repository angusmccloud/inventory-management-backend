/**
 * Shared notification configuration constants
 */

export const NOTIFICATION_CONFIG = {
  immediateWindowMinutes: 15,
  dailyDigestHour: 9, // hour of day in user's timezone
  weeklyDigestDay: 'MONDAY',
  maxEmailsPerRun: 500,
};

export default NOTIFICATION_CONFIG;
